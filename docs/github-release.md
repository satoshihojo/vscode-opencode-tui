# GitHub Release Notes Template

## Summary

- Launch OpenCode directly from the VS Code editor title.
- Route OpenCode edits into a persistent review queue.
- Review and resolve each file with native diffs plus `Keep` / `Undo` controls.

## Included Artifact

- `opencode-tui-integration-<version>.vsix`

## Verification

- `npm run verify`
- `npx @vscode/vsce package`
