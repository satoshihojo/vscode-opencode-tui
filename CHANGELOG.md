# Changelog

## 0.1.0

- Initial public GitHub pre-release of OpenCode TUI Integration.
- Adds a VS Code button that launches a local OpenCode session with the bridge preconfigured.
- Routes OpenCode `edit`, `write`, and `apply_patch` operations into a persistent review queue with native diffs and `Keep` / `Undo` controls.
